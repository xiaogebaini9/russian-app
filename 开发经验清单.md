# 俄语学习 App 开发经验清单（2026-08-15 复盘）

> 来源：鲸汐（DeepSeek）当日修复 11 项问题 + 汐汐（Claude）验收复核。
> 目的：做新功能时照着避坑。

## 一、功能性 Bug（最容易踩）

1. **DOM id 拼接顺序必须"渲染与查询同源"**
   - 教训：渲染时生成 `qr_${lesson}_${index}`，查询却写 `qr_${index}_${quizId}` → 永远查不到 → 抛 TypeError，功能"看似有但没反应"。
   - 防法：id 统一由**同一个函数/模板**生成，查询处复制同一表达式；或改用事件委托 + data 属性，不拼 id。
2. **本地代理类应用必须早期加 CORS**
   - 教训：proxy.js 不处理 OPTIONS、不带 `Access-Control-Allow-Origin` → 页面一旦用 file:// 或跨来源打开，浏览器预检 404 → 深度翻译 "Failed to fetch"，而走外网的快速模式却正常（外网引擎自带 CORS）——**症状很迷惑**。
   - 防法：API 服务从一开始就返回 `Access-Control-Allow-Origin: *`（开发期）+ 处理 OPTIONS 预检。
3. **"成功"不代表"正确"**
   - 教训：network-first 回填缓存不查 `r.ok` → 在线拿到 404/500 错误页也会被写进缓存，断网时用户看到的是坏页面。
   - 防法：缓存写入前 `if (r.ok)` 守卫。

## 二、架构 / 部署

4. **硬编码地址 → 可配置常量**
   - 教训：`localhost:8765` 硬编码 5 处，打包 APK 后手机上全部失效（localhost 指向手机自己）。
   - 防法：抽 `API_BASE` 常量；Android 模拟器自动 `10.0.2.2`；真机在设置页填局域网 IP；默认值兜底本地。
5. **Service Worker 缓存版本必须跟发版绑定**
   - 教训：cache-first 策略下 index.html 被永久缓存，改完代码不换缓存名 → 用户永远拿旧版，**越修越像没修**。
   - 防法：发版必升 `CACHE` 版本号；HTML 用 network-first（在线拿最新，断网落缓存）；配合 `skipWaiting()` + `clients.claim()` 当场接管。
6. **多份静态拷贝必须同步**
   - 教训：root / `www/` / `android assets/` 三份拷贝，只改一份 → APK 里还是旧版。
   - 防法：cap sync 流程化，或改完立刻三份同步（哈希比对校验）；CI 里加"拷贝一致性检查"。
7. **版本号显示要和 git 一致**
   - 教训：设置页写 `v5.0`，git 已 v6 → 用户和测试都困惑。
   - 防法：版本号单一来源（如 package.json 或构建注入），页面显示引用它。

## 三、安全

8. **HTML 转义函数要覆盖引号**
   - 教训：`escape()` 只转义 `<>&`，用户输入拼进 `value="..."` 属性时可闭合属性注入。
   - 防法：属性上下文补 `"` `'` 转义；输入回显优先 `textContent`。
9. **不可信内容进 innerHTML 前必须转义**
   - 教训：`e.message`（来自代理/API）直接拼 `innerHTML` 三处。
   - 防法：统一 escape 或 `textContent`。

## 四、性能 / 交互

10. **高频重渲染必须防抖**
    - 教训：搜索框每敲一键全量重渲染整本书 → 卡顿。
    - 防法：350ms debounce；只重渲染变化的列表；注意输入法 composition 事件。
11. **语音识别"句子没 finalize 就不翻译"是坑**
    - 教训：SpeechRecognition 只在 isFinal 才触发翻译，短句/停顿永远不输出。
    - 防法：interim 结果 + 1.5s 停顿兜底；`lastSent` 去重防重复翻译；处理 `no-speech` 错误。

## 五、AI 协作经验（鲸汐 × 汐汐）

12. **让另一个 AI 当"验货官"非常有效**：她抓到了 4 个我漏的问题（SW 版本、三份拷贝不同步、widget 硬编码、版本号不一致）。
13. **跨进程自动化聊天管道**：键盘注入（AttachThreadInput + keybd_event + 剪贴板粘贴）可以可靠地驱动可见终端里的 Claude Code；AI 干活时会弹工具权限确认，需要盯屏批准；读取 AI 输出用截屏 + Windows OCR。
14. **不要盲杀进程**：清理孤儿进程前先确认归属，误杀 DSH/宿主进程会中断自己的执行管道。
