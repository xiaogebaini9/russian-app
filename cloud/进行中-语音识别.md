# ⏸️ 断点续做说明（语音识别批次）

> 写于 2026-09-02 深夜。本批次代码已全部落盘并部署，**未提交 git、未推 APK**。
> 若会话因额度中断，换模型后说「继续 russian-app 语音识别批次，看 cloud/进行中-语音识别.md」即可。

## 已完成并部署 ✅（线上 https://aimisi.top 已生效）

1. **云语音识别**（服务端 cloud/worker.js）：
   - `POST /api/voice` → Workers AI `@cf/openai/whisper-large-v3-turbo`（实时，快）
   - `POST /api/voice-hq` → `@cf/openai/whisper-large-v3`（完整版，准）
   - 输入格式：**base64 字符串**（`{audio: b64}`）——数组/ArrayBuffer 会报 schema 错，已踩坑
   - 单段限 8MB；实测 wav 转写成功（"Hello, this is a test..."）
   - wrangler.toml 已加 `[ai] binding = "AI"`；AI 免费额度每天 1 万 neurons
2. **客户端**（index.html）：
   - 录音格式优先 `audio/mp4;codecs=mp4a.40.2`，回退 webm/opus（Whisper 认 mp4/aac 更稳）
   - 课堂全程录音自动拼接，结束存 IndexedDB（store 'recordings'，含 blob），并显示绿色「💾 保存录音」按钮 → 浏览器下载 .m4a 文件
   - 识别失败的片段（最多 20 段）在课堂结束时自动用 HQ 模型重试
   - 课堂页引擎状态显示「☁️ 云 Whisper · 免费」（voice-health 返回 device:'cloud'）
3. 隐私政策已更新（录音仅存设备本地）。

## 待办（按顺序）

- [x] ~~**v2：回放模式重翻**~~（2026-09-03 完成）：课堂页新增「📼 回放重翻」按钮——刚结束的课可一键用本课录音重翻，也可导入 .m4a/.webm 录音文件。实现：`<audio>.captureStream()` → MediaRecorder → 复用静音分段逻辑 → 逐段 `/api/voice-hq`（完整版模型）→ class-translate；HQ 失败段收尾用 turbo 补识。回放期间显示状态，结束后生成新会话记录（原课堂记录保留）。
- [ ] 若免费 AI 额度吃紧：加 Groq whisper API 做备源（需用户注册 Groq 拿 key），或开 Workers AI 付费
- [ ] 课堂录制超过 3 小时的内存风险：sessionChunks 全在内存，可后续改为边录边写 IndexedDB
- [ ] APK 内导出录音的下载按钮依赖 a[download]，Capacitor WebView 可能不支持——需要 @capacitor/filesystem 插件（录音已存 IndexedDB 不丢）

## 本批次改动的文件

- cloud/worker.js（voice/voice-hq/voice-health、试用/反馈/剩余次数等上一批）
- cloud/wrangler.toml（[ai] 绑定 + routes aimisi.top）
- index.html / www/ / android assets（客户端全部改动，sw.js 已 v46）
- privacy.html、gen-codes.js、cloud/管理说明.md

## 未提交的 git 变更

上一批（试用/反馈/讲解/手动确认）+ 本批（云语音/录音）都还只在工作区，一次 commit 即可。
